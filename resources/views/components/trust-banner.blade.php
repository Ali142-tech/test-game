<section class="trust-banner" {{ $attributes }}>
    <p class="trust-banner__eyebrow">{{ __('site.trust.eyebrow') }}</p>
    <div class="trust-banner__grid">
        <div class="trust-banner__item">
            <strong>{{ __('site.trust.protected_title') }}</strong>
            <span>{{ __('site.trust.protected_text') }}</span>
        </div>
        <div class="trust-banner__item">
            <strong>{{ __('site.trust.sold_title') }}</strong>
            <span>{{ __('site.trust.sold_text') }}</span>
        </div>
    </div>
</section>
